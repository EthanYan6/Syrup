<!--
  Syrup Firmware
  Copyright (c) 2026 BD1AHN
  Project: 小甜水 (Syrup)
  https://ethanyan6.github.io/Syrup/
-->

# 小甜水使用手册（简版）

官方刷机页：<https://ethanyan6.github.io/Syrup/>  
仓库：<https://github.com/EthanYan6/Syrup>

## 0. 特色一览

| 能力 | 说明 |
|------|------|
| **中英界面** | 菜单 **语言** 可切换；汉字依赖 SPI 字库 |
| **双 PTT** | 硬件 PTT→CH1，侧键 1→CH2 |
| **接收模式** | 仅主信道 / 双频守候 / 跨段 / 主发双收 / **三守** |
| **Yan ID** | 松开 PTT 后发 FSK 呼号；对方主页显示来电 ID |
| **飞机雷达** | 网页 ADS-B 推送到机身 + 空管 AM 收听、数字键输频 |
| **浏览器工具** | 刷固件 / 字库 / 校准 / 写频 / 基础信息 / 飞机雷达 |
| **Fusion 能力** | 频谱、FM、Fox Hunt、BEAM、游戏等（见 README） |

导航键方向跟随菜单 **导航键（SetNav）**：UV-K1 为左/右布局，UV-K5(8) 为上/下布局。

## 1. 浏览器要求

请使用 **Chrome / Edge / Opera**（需 Web Serial）。

## 2. 推荐刷机顺序

1. **备份校准**（原厂或首次刷入前备份一次即可）
2. **刷固件**（关机后按住 PTT 开机进入 BOOT）
3. **刷字库**（正常开机界面下刷入 `cn_font.bin`，地址 `0x024000`）
4. **恢复校准**（本站读写固定 **`0xB000`**）
5. **写频**（按需，正常开机界面）
6. **基础信息**（按需：上行码 / 下行码 / 开机页最后一行，正常开机界面）

## 3. 注意事项

- 仅适用于泉盛 **UV-K1 / UV-K5(K6) V3**。老版本 K5/K6 可能变砖。
- 刷固件需 BOOT 模式；刷字库、校准、写频、基础信息均在**正常使用界面**下连接 USB。
- 校准备份/恢复：本站固定使用 **0xB000**（512 字节，v5 校准区）。
- 字库与叮咚鸡同源格式，有更新时请重新刷入。

## 4. 双 PTT

开启后：硬件 PTT 打 **CH1**，侧键 1 打 **CH2**。谁在发射，主页对应信道就会反色，松开后主信道留在刚发射的那条，可继续操作。

### 如何开启

1. 打开菜单，进入 **侧键1短按**（或 **侧键1长按**）。
2. 选 **PTT**，确认保存。另一个槽位会自动同步为 PTT。
3. 建议把菜单里的 **设置 PTT** 设为 **经典**（按住说话）。双 PTT 开启后固件也会把它改回经典。

**PTT** 只出现在侧键 1 的短按/长按里。侧键 2、MENU 长按列表没有这一项。

不要和 **设置 PTT**（经典 / 一键）搞混：那一项只改**硬件 PTT** 是按住说话还是按一下开关，不会打开双 PTT。

### 怎么用

| 按键 | 效果 |
|------|------|
| 硬件 PTT 按住 | CH1 发射，主页 CH1 反色 |
| 侧键 1 按住 | CH2 发射，主页 CH2 反色 |
| 两键同时按住 | 硬件 PTT 优先，打 CH1 |
| 松开 | 停止发射 |

侧键 1 整颗都当 CH2 的 PTT（按下发射、松开停止）。开启后，侧键 1 上原来绑的短按/长按动作不会再触发。

### 如何关闭

把 **侧键1短按** 或 **侧键1长按** 改成别的动作（例如「无」）。两个槽位会一起变成 **无**，之后可以再分别设置。

## 5. 接收模式

菜单 **接收模式**（英文 RxMode）控制主页信道怎么听、怎么打。前四种主页都一样：始终显示 **CH1 / CH2**，反色永远是主信道。差别在收音机听哪条、按 PTT 打哪条，以及波形 / S 表 / 上次接收 / DTMF 跟谁走。第五项 **三守** 改成三行，按键和主页都不一样，见下一节。

| 选项 | 听 | 打 | 主页上你会看到 |
|------|----|----|----------------|
| **仅主信道** | 只听反色那条 | 打反色那条 | 另一条只是摆着。波形、S 表、DTMF、底部「上次接收」都只会出现在反色信道 |
| **双频守候** | CH1 / CH2 约 100ms 轮询；有信号就停在那条 | **打刚听到的那条**（应答） | 副信道有信号时，反色仍在主信道，但波形 / S 表 / DTMF / 上次接收在真正有信号的那行。松开后约 1 秒再继续轮询；刚发射过会停约 4 秒 |
| **跨段** | **只听非主信道** | **永远打反色主信道** | 反色在 CH1 时，实际在听 CH2。波形、S 表、DTMF 会出现在没反色的那行 |
| **主发双收** | 和双频守候一样两边都听 | **永远打反色主信道** | 听起来像双守候，但按 PTT 不会跟到刚收到的那条。收完停得更短（大约 1 秒）就继续轮询 |
| **三守** | CH1 / CH2 / CH3 约 100ms 轮询；有信号就停在那条 | **硬件 PTT→CH1，侧键 1→CH2，侧键 2→CH3** | 主页三行，没有波形条。真正收到的那行会闪 **RX** 并显示 S 表 |

和主页控件的关系：

- **反色** = 主信道，不跟接收走。只有切主信道、双 PTT（硬件 PTT→CH1，侧键 1→CH2）或三守（再加侧键 2→CH3）才会换反色。
- **底部波形 / S 表** = 当前正在解调的那条。
- **DTMF** = 记在真正收到码的那条上，超时前不会因为轮询或反色跳走。
- **底部喇叭 + 信道名** = 最近一次打开静噪的那条，不看主信道。

所以：前四种主页都是双行，区别是「字画在哪、声从哪来、按 PTT 打哪」。双频守候是听哪打哪；跨段 / 主发双收是听可以在副信道，打一定在反色主信道。三守单独占一节。

## 6. 三守

开启后主页变成 **CH1 / CH2 / CH3** 三行，约 100ms 轮询三条；有信号就停在那条听。发射不跟刚收到的那条走，而是三颗键各打一条：硬件 PTT 打 CH1，侧键 1 打 CH2，侧键 2 打 CH3。谁在发射，对应那行就会反色，松开后主信道留在刚发射的那条，可继续改频、改信道。

和双 PTT 互斥：开三守会关掉双 PTT，并把侧键 1 / 侧键 2 的短按、长按都清成「无」。菜单里这四项也会藏起来。跨段会被强制关掉。侧键 2 在发射中发 1750Hz 的功能也停用。

### 如何开启

1. 打开菜单，进入 **接收模式**（英文 **RxMode**）。
2. 选 **三守**（英文 **TRI WATCH**），确认保存。

CH3 是第三条独立 VFO，可设频率或记忆信道，默认约 **433.500 MHz**，写入 Flash，重启还在。

### 怎么用

| 按键 | 效果 |
|------|------|
| 硬件 PTT 按住 | CH1 发射，主页 CH1 反色 |
| 侧键 1 按住 | CH2 发射，主页 CH2 反色 |
| 侧键 2 按住 | CH3 发射，主页 CH3 反色 |
| 两键同时按住 | 硬件 PTT 优先，打 CH1；松开 PTT 且仍按着侧键时，改打那条侧键对应的信道 |
| **F + 2** | 主信道在 CH1 → CH2 → CH3 循环（反色跟着走） |
| 松开 | 停止发射 |

侧键 1、侧键 2 整颗都当对应信道的 PTT（按下发射、松开停止）。开启后原来绑在这两颗键上的短按 / 长按不会再触发。

主页上改频率、切记忆信道、数字键输频，都只作用于当前反色那一行。导航键方向仍跟随菜单 **导航键（SetNav）**。

### 主页上你会看到

- **反色** = 主信道，也是刚打过或刚用 F+2 切到的那条。
- 有信号的那行左侧会闪 **RX**，旁边显示 **S 表**；信号消失后 RX 标记还会留一会儿，直到你改了那一行的频率或信道。
- 没有底部波形条，第三行占了原来波形的位置。低电量或键盘锁提示出现时，会暂时盖住 CH3 那一行。
- **DTMF**、**Yan ID** 仍记在真正收到的那一行上。

听的节奏和双频守候类似：轮询间隔约 100ms，收完大约停 1 秒再转，刚发射过大约停 4 秒。

### 如何关闭

把 **接收模式** 改回其他四项之一。若当时主信道在 CH3，会回到 CH1。之后可以重新给侧键 1 / 侧键 2 分配短按、长按动作。

## 7. Yan ID

松开 PTT 后，不发普通 Roger / MDC 尾音，而是发一段短 FSK，把本机最多 6 位呼号送出去。协议与叮咚鸡 / mangosteen 的 GGM2 Yan ID 兼容：对方同样开着 Yan ID 接收时，主页对应信道会显示来电呼号。

### 如何开启

1. 打开菜单，进入 **发射尾音**（英文 **Roger**），选 **Yan ID**，确认。
2. 进入 **Yan ID**，编好自己的呼号后保存。没写呼号就不会发。
3. 要看别人的呼号：再进 **接收 Yan ID**（英文 **Yan Rx**），选 **开**。这一项只有在发射尾音已经是 Yan ID 时才会出现。

### 怎么编呼号

最多 **6** 位，保存时只保留 **A–Z** 和 **0–9**（自动转大写，空格丢掉）。编辑方式和信道名类似：

| 按键 | 效果 |
|------|------|
| **2–9** | 选字母（出现候选后用 **1–4** 确认） |
| **F / #** | 字母大写 ↔ 数字 |
| **0–9** | 数字模式下直接输入数字 |
| **EXIT** | 退格；第一位再按一次退出编辑且不保存 |
| **MENU** | 保存 |

### 怎么用

发射：对着麦克风说话，松开 PTT 即可。本机已设呼号时，尾音阶段会发出 Yan ID。对方不用按任何键。

接收：两边要在同一频率上，发射尾音都是 Yan ID，并且接收方开了 **接收 Yan ID**。收到后，该信道名称位置会换成对方呼号，左侧带一个小电话图标，大约 **6 秒** 后恢复原来的信道名。自己刚发出去的包会被忽略，主页不会回显自己。

发、收这段 FSK 时手台会暂时切到窄带，发完 / 收完再改回来，不用手动改带宽。

呼号存在 SPI Flash，拔线、重启还在。恢复出厂或擦掉相关扇区后会清空。

## 8. 飞机雷达

手台可单独打开「飞机雷达」页面：顶部为与 FM 相同的状态栏；第一行左侧飞机图标、右侧反色大字航班号；下方左对齐均匀排列高度、距离、空管 AM 频率。页面文案跟随菜单 **语言**（中文 / 英文）。

默认频率一般为 **121.500 MHz**（或网页推送时保留机内已设频率）。可用导航键步进，也可用数字键直接输入。

### 如何打开手台页面

1. 打开菜单，进入任意侧键短按 / 长按（或 MENU 长按）动作设置。
2. 选 **飞机雷达**（英文 **AIRCRAFT RADAR**），确认保存。
3. 按下该侧键即可进入页面；再按一次同一动作可退出回主页。页面内按 **EXIT** 也回主页。

### 网页推送到手台

1. 用 `python docs/serve.py` 打开本站（浏览器无法直连 ADS-B 接口，需本机代理）。
2. 手台**正常开机**进入使用界面（不要进 BOOT）。
3. 打开 **飞机雷达探测** 标签，点 **连接手台**，在浏览器里选中串口。
4. 允许定位并等待扫描后，**点选一架飞机**，或先点选再点 **推送到手台**：呼号 / 高度 / 距离会推到机身，并自动打开飞机页、切入空管 **AM** 收听。

未连接时点选只会提示先连接，不会改动机内数据。ADS-B **不含**塔台/进近频率，推送时频率字段为 0，手台保留当前空管频。

### 收听与按键

| 按键 | 效果 |
|------|------|
| 进入页面 / **MENU** | 按当前频率打开 AM 收听（空管波段 108–137 MHz） |
| **0–9** | 直接输入频率（6 位，如 `121500`；输入中频率行显示预览，未满位用 `-`） |
| **\*** | 未输入时：回到 121.500；输入中：当作小数点（如 `121*500`） |
| **导航键** | 按步进改频并重调 AM（输入中会先取消输入）。方向跟随菜单 **导航键**：K1 左/右、K5 上/下 |
| **长按 F** | 锁定 / 解锁键盘（状态栏小锁图标；页面不弹提示） |
| **EXIT** | 输入中：退格；无输入：回主页（频率保持，可继续在主界面听） |

超时未输完会取消输入，恢复显示已锁定频率。

### 拔线后仍保留

选中目标会写入机内 Flash。拔掉 USB、重启后，再打开飞机雷达页仍可看到上次航班信息。恢复出厂 / 擦除相关扇区后会清空。

## 9. 更多

完整功能与上游 Fusion 能力见仓库 [README](https://github.com/EthanYan6/Syrup)。维护者：**BD1AHN**。
