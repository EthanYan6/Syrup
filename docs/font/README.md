# Syrup CN font

`cn_font.bin` is the Chinese font blob flashed to SPI address `0x024000`
(same layout as Dondji / App/settings.h `CN_FONT_*`).

A copy also lives in `docs/fonts/cn_font.bin` for alternate fetch paths.
